1.SELECT E1.FIRST_NAME,E1.LAST_NAME,D1.DEPARTMENT_ID, D1.DEPARTMENT_NAME, C.CITY'CITY_NAME'FROM departments D1 LEFT JOIN employees E1 ON(D1.MANAGER_ID=E1.EMPLOYEE_ID) AND  `DEPARTMENTS` D1 JOIN LOCATIONS C ON (D.LOCATION_ID = C.LOCATION_ID)

2.SELECT FIRST_NAME,LAST_NAME,DEPARTMENT_ID,SALARY FROM `employees` WHERE IN (SELECT MIN(SALARY)FROM employees WHERE DEPARTMENT_ID IN (SELECT DEPARTMENT_ID FROM employees WHERE SALARY=EMPLOYEE_ID)) ORDER BY SALARY ACS