1.SELECT CONCAT ( E1.FIRST_NAME,E1.LAST_NAME)'FULL_NAME',E1.EMPLOYEE_ID,D.DEPARTMENT_ID,E1.salary FROM `employees` E1 join departments D ON (E1.DEPARTMENT_ID = D.DEPARTMENT_ID) WHERE SALARY > ALL ( SELECT SALARY FROM employees WHERE DEPARTMENT_ID = 50)


2.SELECT * FROM employees WHERE HIRE_DATE<ALL(SELECT HIRE_DATE FROM employees WHERE DEPARTMENT_ID=30)